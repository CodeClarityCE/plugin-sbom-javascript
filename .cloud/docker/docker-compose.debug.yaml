services:
  plugin-js-sbom:
    # For dev we keep the golang container and install devtools
    build:
      target: plugin-debug
    image: ceherzog/js-sbom-debug:latest
    ports:
      - 40000:40000
